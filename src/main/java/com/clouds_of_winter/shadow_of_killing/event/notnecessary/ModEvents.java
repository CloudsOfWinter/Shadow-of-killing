package com.clouds_of_winter.shadow_of_killing.event.notnecessary;

import net.minecraftforge.fml.common.Mod;



@Mod.EventBusSubscriber
public class ModEvents {
    /*
    @SubscribeEvent
    public void registerCapabilities(RegisterCapabilitiesEvent event) {
        event.register(TargetData.class);
    }

    @Mod.EventBusSubscriber(modid = Shadowofkilling.MOD_ID, bus = Mod.EventBusSubscriber.Bus.FORGE)
    public static class ForgeEvents {
        @SubscribeEvent
        public static void onAttachCapabilities(AttachCapabilitiesEvent<Entity> event) {
            ModCapabilities.onAttachCapabilities(event);
        }

        @SubscribeEvent
        public static void onLivingTick(LivingEvent.LivingTickEvent event) {
            CombatHandler.onLivingTick(event);
        }

        @SubscribeEvent
        public static void onSetTarget(LivingChangeTargetEvent event) {
            CombatHandler.onSetTarget(event);
        }

        @SubscribeEvent
        public static void onAttack(LivingAttackEvent event) {
            CombatHandler.onAttack(event);
        }
    }
    */



   



}
